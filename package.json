{
  "name": "",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format": "prettier --write .",
    "build": "esbuild src/script.ts --bundle --outdir=dist",
    "build:watch": "esbuild src/script.ts src/mcts.worker.ts --bundle --outdir=dist --watch --servedir=."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.25.8",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3"
  }
}
